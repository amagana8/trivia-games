import { css } from '@mui/material-pigment-css';

export const menuScreenStyles = css(({ theme }) => ({
  alignItems: 'center',
  display: 'flex',
  gap: theme.spacing(2),
  justifyContent: 'center',
}));
